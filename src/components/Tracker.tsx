const Tracker = () => {
  return (
    <div>
      <span>Tracker name</span>
      <span>Time</span>
      <button>Stop</button>
      <button>Delete</button>
    </div>
  );
}

export default Tracker;
